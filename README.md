

 📋 Описание проекта
Реализация структуры данных "Стек" (Stack) и её применение для проверки сбалансированности скобок в строках.

Проект содержит решение двух задач:
1. **Реализация класса Stack** с полным набором методов
2. **Проверка сбалансированности скобок** с использованием созданного стека

## 🚀 Установка и запуск

### Требования
- Python 3.6 или выше

### Запуск программы
```bash
# Скачайте файл
git clone <ваш-репозиторий>

# Перейдите в директорию проекта
cd <директория-проекта>

# Запустите программу
python stack.py


📁 Структура проекта
text
├── stack.py          # Основной файл с реализацией всех заданий
├── README.md         # Документация (этот файл)
└── .gitignore        # Файл для игнорирования ненужных файлов в Git


🎯 Задание 1: Реализация класса Stack
Класс Stack реализует следующие методы:
Метод	Описание	Возвращаемое значение
__init__()	Инициализация пустого стека	-
is_empty()	Проверка стека на пустоту	True или False
push(item)	Добавляет элемент на вершину стека	-
pop()	Удаляет и возвращает верхний элемент	Удаленный элемент
peek()	Возвращает верхний элемент без удаления	Верхний элемент
size()	Возвращает количество элементов в стеке	Целое число
Пример использования:
python
stack = Stack()
stack.push(1)
stack.push(2)
stack.push(3)
print(stack.pop())  # 3
print(stack.peek())  # 2
print(stack.size())  # 2


🎯 Задание 2: Проверка сбалансированности скобок

Функции:
is_balanced(brackets_string) - проверяет баланс скобок, возвращает True/False

check_brackets(brackets_string) - возвращает понятное сообщение о результате

Поддерживаемые скобки:
Круглые: ( )

Квадратные: [ ]

Фигурные: { }

Примеры сбалансированных последовательностей:
text
(((([{}]))))
[([])((([[[]]])))]{()}
{{[()]}}
Примеры несбалансированных последовательностей:
text
}{}
{{[(])]}}
[[{())}]


🧪 Тестирование
Автоматические тесты
Программа содержит встроенные тесты из условия задания:

3 сбалансированные последовательности ✓

3 несбалансированные последовательности ✓

Дополнительные тестовые случаи ✓

Интерактивный режим
После запуска программы доступен интерактивный режим для проверки произвольных строк:

text
Введите строку со скобками (или 'выход' для завершения): ([]{})
Результат: Сбалансированно
🛠️ Технические детали
Особенности реализации
Обработка ошибок: При попытке удалить/посмотреть элемент из пустого стека выбрасывается исключение IndexError

Защищенные атрибуты: Используется _items для инкапсуляции данных стека

Строковое представление: Класс Stack имеет метод __str__ для удобной отладки

Алгоритм проверки скобок
Создается пустой стек

Проходим по каждому символу строки:

Если символ - открывающая скобка: добавляем в стек

Если символ - закрывающая скобка:

Проверяем соответствие с верхним элементом стека

Если не соответствует или стек пуст - строка несбалансирована

Если соответствует - удаляем из стека

После обработки всех символов стек должен быть пуст



📊 Пример вывода программы
text
============================================================
Демонстрация работы стека:
============================================================
1. Создаем стек и добавляем элементы:
   Стек после push(1), push(2), push(3): [1, 2, 3]
   Размер стека: 3
   Верхний элемент (peek): 3
   Стек пуст? False

2. Удаляем элементы:
   pop(): 3
   Стек теперь: [1, 2]
   pop(): 2
   Стек теперь: [1]

============================================================
Проверка сбалансированности скобок:
============================================================
✓ '(((([{}]))))             ' -> Сбалансированно
✓ '[([])((([[[]]])))]{()}   ' -> Сбалансированно
✓ '{{[()]}}                 ' -> Сбалансированно
✓ '}{}                      ' -> Несбалансированно
✓ '{{[(])]}}                ' -> Несбалансированно
✓ '[[{())}]                 ' -> Несбалансированно

============================================================
Интерактивная проверка:
============================================================

Введите строку со скобками (или 'выход' для завершения): 